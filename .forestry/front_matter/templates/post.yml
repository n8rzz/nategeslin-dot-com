---
label: Post
hide_body: false
fields:
  - type: text
    name: title
    label: title
    config:
      required: true
  - name: author
    type: select
    default: Nate Geslin
    config:
      required: false
      options: []
      source:
        type: documents
        section: author-list
        file: data/authors/authors.json
        path: authors
    label: author
    description: Choose author's name, add new author via Author List in  Sidebar
  - type: datetime
    name: date
    label: date
    config:
      required: true
    default: now
  - type: file
    name: hero_image
    label: hero_image
  - type: text
    name: summary
    label: summary
    config:
      required: true
  - type: boolean
    name: Draft
    label: Draft
    config:
      required: true
